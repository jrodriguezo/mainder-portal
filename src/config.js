export const PATHS = {
  HOME: "/",
};

export const BASE_URL = "https://api.mainder.ai";

export const VERSION = "/api/v1";

export const BASE_URL_VERSION = BASE_URL + VERSION;

export const ENDPOINTS = {
  AGENCIES: "/agencies",
};
